# Copy this file to .env and fill in your values (optional)
# The Claude Agent SDK uses Claude Code authentication, so no API key is needed
# if you're logged in via `claude login`

PORT=8787

# Verbose logging is ON by default. Disable with:
# VERBOSE=false
