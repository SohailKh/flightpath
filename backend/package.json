{
  "name": "src",
  "type": "module",
  "scripts": {
    "dev": "npx tsx --watch src/index.ts",
    "dev:bun": "bun run --watch src/index.ts",
    "dev:wrangler": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "bun test",
    "playwright:smoke": "bun run src/scripts/playwright-smoke.ts",
    "playwright:screenshot": "bun run src/scripts/playwright-screenshot.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.5",
    "@hono/node-server": "^1.13.0",
    "hono": "^4.11.3",
    "playwright": "^1.57.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsx": "^4.19.0",
    "wrangler": "^4.4.0"
  }
}